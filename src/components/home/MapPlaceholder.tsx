import styles from '@/styles/home/MapPlaceholder.module.css'

export default function MapPlaceholder() {
  return (
    <div className={styles.map}>
      <span className={styles.label}>Mapa (integração futura)</span>
    </div>
  )
}
